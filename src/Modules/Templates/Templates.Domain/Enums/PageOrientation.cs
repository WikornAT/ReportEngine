namespace Templates.Domain.Enums;

/// <summary>Page orientation used when generating PDF output.</summary>
public enum PageOrientation
{
    /// <summary>Portrait (tall) orientation — default.</summary>
    Portrait = 0,

    /// <summary>Landscape (wide) orientation.</summary>
    Landscape = 1,
}
